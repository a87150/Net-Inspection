# PC 远程日志改造交付记录

日期：2026-09-07。工作目录：net；分支：codex/pc-remote-log-ingestion-recovery。

## 已实现

- 单一汇总来源，SMB 或 FTP/FTPS；管理员保存、连接测试与只读预览。
- 文件日期范围、独立终端写入路径、远程已处理/失败目录、本地暂存。
- Windows/macOS 每日一次 JSON 发布；先临时写入再改名，无旧 HTTP 上传。
- 获取任务互斥、独立连接有界下载、串行事务入库、多线程分析。
- 每台 PC 每个日志日期第一份有效证据；去重、归档恢复、取消与租约保护。
- 获取重试耗尽后保留独立分析交接，失败任务的原始结果不可变。
- 新分析规则及冻结人员/部门/OU/站点信息；详情、筛选与导出接通。
- 旧上传接口、本地扫描器、旧页面别名和旧上传脚本已移除；历史归档表保留。

## 验证

- 全量 Django 测试：961 项通过，耗时约 149 秒。
- 前端行为测试：23 项通过。
- Django check、makemigrations --check --dry-run、git diff --check 通过。
- PowerShell 采集模板只读语法检查通过。
- 使用实际 Worker 线程和模拟连接验证获取、分析、告警及归档全链路。
- deploy.demo 的现有数据库迁移至 0029，静态资源已重新收集。
- 通过 seed_pc_remote_demo 补充 1 条离线获取任务、4 条传输记录；未覆盖已有人员/设备。

## 未执行的真实环境验证

没有真实 SMB/FTP/FTPS 登录、真实域控调用或 Windows/macOS 终端采集；未连接 MySQL 验证。测试使用 SQLite 和模拟网络连接。

重启原有 deploy.demo Web/Worker 后，管理员保存实际来源设置，先测试连接、预览目录，再下载并部署新脚本。检查两端路径确实对应同一汇总目录，再手动执行获取。

不要删除 demo-runtime 中的凭据加密密钥。旧历史归档记录保留在数据库；已删除代码可从 Git 恢复。
